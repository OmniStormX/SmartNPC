---
description: 
alwaysApply: true
enabled: true
updatedAt: 2026-04-29T08:41:30.036Z
provider: 
---

# Windows 环境注意

## Shell

- 当前 shell 是 `cmd.exe`，不是 PowerShell
  - 跨盘切目录用 `d: && cd d:\path && <cmd>` 形式
  - 不要用 PowerShell 专属语法（`Set-Location`、`;` 链式、`$env:` 等）
- 路径用反斜杠 `\` 或正斜杠 `/` 都可以；写到 .go 文件里的 string literal 用 `\\` 或 raw string
- **环境变量改动后，当前 cmd 窗口 inherit 的还是旧值**：
  - 用 `cmd /d /c "..."` 起干净子进程验证真实状态
  - 让用户重开 cmd / 重启 VS Code 才会生效

## Go 工具链

- 此机器 `GOROOT=C:\Program Files\Go`，`GOPATH=C:\Users\synchen\go`（标准布局）
- **如果看到 `warning: both GOPATH and GOROOT are the same directory`**：
  - 不要直接 `go env -w GOPATH=...`——这写到 `%APPDATA%\go\env`，**优先级低于环境变量**，无效
  - 必须先查 4 个位置确认 GOPATH 在哪被设：
    ```cmd
    reg query "HKCU\Environment" /v GOPATH 2>nul
    reg query "HKLM\SYSTEM\CurrentControlSet\Control\Session Manager\Environment" /v GOPATH 2>nul
    type "%APPDATA%\go\env"
    echo %GOPATH%
    ```
  - 用户级冲突：`reg delete "HKCU\Environment" /v GOPATH /f`
  - 系统级冲突：需要管理员，让用户在 "系统属性 → 环境变量" 里删
  - 修完用 `cmd /d /c "go version & go env GOPATH & go env GOROOT"` 验证（必须新进程）
- Go 1.10+ 不需要手动设 `GOROOT`，自动从 `go.exe` 推导，**遇到设了 GOROOT 的也建议删**

## Device Guard / WDAC

> 历史上此机器拦过 `%USERPROFILE%\go\bin\` 下的 exe（`task.exe`、`gopls.exe`、`go run` 临时产物等）。
> **截至 2026-04-29 实测已不再拦截**——直接执行成功。但保留以下经验，万一未来策略变化能快速定位。

- **第一时间用最便宜的命令实测**，不要预设拦截：
  ```cmd
  "%USERPROFILE%\go\bin\<tool>.exe" --version
  ```
  能正常打印就是没被拦；报"已被组织的 Device Guard 策略阻止"才是真拦了
- 如果**历史行为**确认仍被拦（弹框报错、`Error: spawn UNKNOWN`）：
  - 优先方案：把工具装到受信任目录（`C:\Program Files\Go\thirdparty\`）
  - 备选：联系 IT 把 `%USERPROFILE%\go\bin\` 加 WDAC 白名单
  - 临时绕：用签名版（如 `winget install Task.Task` 而非 `go install go-task/...`）
- `go.exe` 本体（在 `C:\Program Files\Go\`，签名）始终被信任 —— `go build` / `go test` / `go vet` 永远可用，不受策略影响
- **不要预设拦截**写绕开代码或临时脚本——验证后再说

## 文件编码

- **所有文本文件统一 UTF-8（无 BOM）**：`.go` / `.md` / `.yml` / `.py` / `.cs` 等都不许 GBK
- `write_to_file` 工具在 Windows 上偶发把含中文的文件写成 GBK；写完含中文的文件后必须自检：
  ```cmd
  python -c "open(r'PATH','rb').read().decode('utf-8'); print('utf-8 ok')"
  ```
  失败立刻用 `python -c "data=open(r'PATH','rb').read(); open(r'PATH','wb').write(data.decode('gbk').encode('utf-8'))"` 修正
- 纯 ASCII 文件（无中文）不需要自检
- 行尾不强制 LF；Windows CRLF 也接受，由 `.gitattributes` 后续统一