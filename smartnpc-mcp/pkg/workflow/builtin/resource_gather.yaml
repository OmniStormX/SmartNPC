id: resource_gather
description: 资源采集——Agent 根据 smartnpc-farm-gather skill 动态决策
version: "6"
inputs:
  - name: inspect_radius
    description: 巡视半径
    default: 30
steps:
  - kind: skill_call
    skill: smartnpc-farm-gather
    args:
      inspect_radius: "$inspect_radius"
