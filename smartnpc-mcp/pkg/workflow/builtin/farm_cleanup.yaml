id: farm_cleanup
description: 农场清理——Agent 根据 smartnpc-farm-cleanup skill 动态决策
version: "5"
inputs:
  - name: inspect_radius
    description: 巡视半径
    default: 30
steps:
  - kind: skill_call
    skill: smartnpc-farm-cleanup
    args:
      inspect_radius: "$inspect_radius"
